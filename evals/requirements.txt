# Eval-only dependencies (TIP-009) — NOT part of the agent runtime.
# The golden runner needs only httpx + supabase (already in the agent venv).
# RAGAS adds the heavy judge/metric stack; install in a separate eval venv:
#   python -m venv .evalvenv && .evalvenv\Scripts\pip install -r evals/requirements.txt
httpx
supabase>=2.31.0
# ragas 0.2.15 eagerly imports langchain_community.chat_models.vertexai, removed
# in langchain-community>=0.3 → pin community to 0.2.17 (still ships that module).
# This exact combo was verified end-to-end on TIP-009.
ragas==0.2.15
langchain-community==0.2.17
langchain-anthropic>=0.3.0
langchain-huggingface>=0.1.0
FlagEmbedding>=1.4.0
