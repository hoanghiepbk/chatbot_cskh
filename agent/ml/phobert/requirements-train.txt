# PhoBERT TRAINING env (TIP-012a) — SEPARATE venv on the GPU machine.
# DO NOT install into the agent's uv env: the agent is pinned to torch-CPU
# (TIP-010.5, for the light Railway image). Training needs torch-CUDA.
#
# Step 1 — install torch-CUDA matching your GPU (check `nvidia-smi` → CUDA version):
#   CUDA 12.4:  pip install torch --index-url https://download.pytorch.org/whl/cu124
#   CUDA 12.1:  pip install torch --index-url https://download.pytorch.org/whl/cu121
#   CUDA 11.8:  pip install torch --index-url https://download.pytorch.org/whl/cu118
# Step 2 — pip install -r requirements-train.txt   (torch intentionally NOT listed
#          here so pip won't overwrite your CUDA wheel with a CPU one)
transformers>=4.40
underthesea>=6.8.0
scikit-learn>=1.3
numpy
onnx>=1.16
onnxruntime>=1.18
onnxruntime-tools>=1.7
onnxscript>=0.1   # torch>=2.9 imports it inside torch.onnx.export (even legacy path)
